# Reference witness for the membership circuit.
#
# secret = 42, leaf_index = 5, all siblings = 7.
#
# `root` is a Pedersen hash and cannot be written by hand. It was produced by
# the circuit itself:
#
#   nargo test --show-output print_reference_witness
#
# Regenerate it with that command after any change to TREE_DEPTH, the hashing,
# or the domain separator.
#
# The nullifier is not listed because it is not an input — the circuit returns
# it, and it appears as the last public input of the resulting proof (ADR-008).

root = "0x089175ccc891f80d0f76bc5c6f7a239c2a78069ddf64478b68410c7d6b4c7320"

secret = "42"
leaf_index = "5"
siblings = [
    "7", "7", "7", "7", "7", "7", "7", "7", "7", "7",
    "7", "7", "7", "7", "7", "7", "7", "7", "7", "7",
]
